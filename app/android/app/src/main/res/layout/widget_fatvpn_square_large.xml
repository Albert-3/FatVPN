<?xml version="1.0" encoding="utf-8"?>
<!-- The 2×2 tile on a launcher that gives it real height.

     "2×2" is not a size. On a Redmi Note 7 (MIUI, 4 columns) two cells measure
     148×204dp — half again as tall as they are wide — and the small square
     layout's 50dp disc left about 55dp of empty card above and below it. The
     tile did not look wrong, it looked *unfinished*.

     So this is the same layout with room spent on it: the product name at the
     top, a power disc the size of the one in the app, and text a step up. It is
     chosen only above 180dp of height (see SQUARE_LARGE_HEIGHT_DP), because
     that is what it needs: 12 + 16 + 8 + 78 + 10 + 21 + 3 + 18 + 12 ≈ 178dp.
     Anything shorter gets widget_fatvpn_square.xml, which fits in 104.

     Same view ids as the other three layouts (the status dot included, as a GONE
     view) so one drawing routine fills any of them. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingTop="12dp"
    android:paddingEnd="12dp"
    android:paddingBottom="12dp">

    <!-- Static: the widget is the only thing on this part of the home screen
         that has no icon next to it saying whose it is. -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:text="@string/widget_name"
        android:textColor="@color/widget_text_secondary"
        android:textSize="12sp"
        android:textStyle="bold" />

    <FrameLayout
        android:id="@+id/widget_power_touch"
        android:layout_width="78dp"
        android:layout_height="78dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/widget_power_ripple">

        <ImageView
            android:id="@+id/widget_power_bg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:src="@drawable/widget_power_off" />

        <ImageView
            android:id="@+id/widget_power"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_gravity="center"
            android:contentDescription="@string/widget_power_description"
            android:src="@drawable/ic_widget_power" />
    </FrameLayout>

    <!-- Present, never shown: the disc above says the same thing in the same
         colour, and a dot beside centred text would only break its symmetry. -->
    <ImageView
        android:id="@+id/widget_dot"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:contentDescription="@null"
        android:src="@drawable/widget_dot"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:text="@string/widget_status_disconnected"
        android:textColor="@color/widget_text_primary"
        android:textSize="16sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/widget_location"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@string/widget_location_best"
            android:textColor="@color/widget_text_secondary"
            android:textSize="13sp" />

        <Chronometer
            android:id="@+id/widget_timer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:textColor="@color/widget_accent"
            android:textSize="13sp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/widget_hint"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:ellipsize="end"
            android:gravity="center"
            android:maxLines="1"
            android:text="@string/widget_hint_tap_to_connect"
            android:textColor="@color/widget_text_secondary"
            android:textSize="13sp"
            android:visibility="gone" />
    </LinearLayout>
</LinearLayout>
