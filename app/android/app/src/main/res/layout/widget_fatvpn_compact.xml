<?xml version="1.0" encoding="utf-8"?>
<!-- Compact layout — one launcher row (roughly 60-100dp of usable height).
     Two text lines and nothing more: a RemoteViews layout cannot measure
     itself, so anything that does not fit is silently clipped rather than
     shrunk, which is exactly how the first build ended up with half a line of
     text sawn off at the widget's bottom edge.

     Same view ids as widget_fatvpn_wide.xml, so FatVpnWidgetProvider fills
     either without knowing which it got. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="14dp"
    android:paddingTop="10dp"
    android:paddingEnd="12dp"
    android:paddingBottom="10dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/widget_dot"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:contentDescription="@null"
                android:src="@drawable/widget_dot" />

            <TextView
                android:id="@+id/widget_status"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="7dp"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/widget_status_disconnected"
                android:textColor="@color/widget_text_primary"
                android:textSize="15sp"
                android:textStyle="bold" />
        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/widget_location"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/widget_location_best"
                android:textColor="@color/widget_text_secondary"
                android:textSize="12sp" />

            <!-- The clock and the call to action share this slot: only one of
                 them is ever visible (see FatVpnWidgetProvider.buildViews). -->
            <Chronometer
                android:id="@+id/widget_timer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:textColor="@color/widget_accent"
                android:textSize="12sp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/widget_hint"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                android:text="@string/widget_hint_tap_to_connect"
                android:textColor="@color/widget_text_secondary"
                android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>

    <FrameLayout
        android:id="@+id/widget_power_touch"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:layout_marginStart="10dp">

        <ImageView
            android:id="@+id/widget_power_bg"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@null"
            android:src="@drawable/widget_power_off" />

        <ImageView
            android:id="@+id/widget_power"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:layout_gravity="center"
            android:contentDescription="@string/widget_power_description"
            android:src="@drawable/ic_widget_power" />
    </FrameLayout>
</LinearLayout>
