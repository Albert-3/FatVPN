<?xml version="1.0" encoding="utf-8"?>
<!-- The touch feedback on the power disc.

     Its own ripple rather than ?android:attr/selectableItemBackgroundBorderless:
     a widget's layout is inflated by the launcher, against the launcher's theme,
     so a theme attribute resolves to whatever that launcher happens to define —
     or to nothing. A drawable of our own resolves to the same thing everywhere.

     The mask is the disc itself (widget_power_off/on are ovals of the same
     size), so the ripple is round and stops at the button's edge instead of
     washing over the square touch target around it. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/widget_ripple">
    <item android:id="@android:id/mask">
        <shape android:shape="oval">
            <solid android:color="@android:color/white" />
        </shape>
    </item>
</ripple>
