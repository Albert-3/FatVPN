<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch splash: dark background + centered logo. Automatically
     removed once the Flutter engine draws its first frame (the animated
     SplashScreen), so the two hand off seamlessly. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
