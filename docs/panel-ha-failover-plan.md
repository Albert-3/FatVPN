# Отказоустойчивость панели Remnawave: три сервера, автопереключение, бэкапы

> Составлено 2026-08-04, после инцидента 2026-08-03: сервер с панелью управления
> (`z.fatvdsnvv.space`) завис/отключился, и встала вся система — при том, что сами
> VPN-ноды продолжали гнать трафик. ~~Бэкапов на сервере панели не было~~
> ✏️ **Поправка от заказчика (2026-08-04): бэкапы ЕСТЬ** — базы и панели, и бота,
> присылаются в Telegram. Исходная вводная «бэкапов нет» была неверна; §2 переписан
> из «создать бэкапы» в «проверить существующие» — что в них попадает, и разворачиваются
> ли они (см. там же, почему это не формальность).
>
> Требование заказчика: **три сервера для панели с автоматическим переключением**
> (умер первый → работает второй → умер второй → работает третий) + **третий
> резервный сервер боту** (у бота уже есть основной + запасной с автопроверкой).
>
> Статус документа: **план, ничего из него не сделано**. Каждый раздел заканчивается
> блоком «Готово, когда» — до выполнения этих пунктов раздел считается несделанным,
> независимо от того, что написано в конфигах. Урок проекта, подтверждённый уже
> трижды (500-е после деплоя `820b1fe`, «зелёный» build 136, кнопка виджета):
> **«настроено» ≠ «работает», работает только то, что пробовали сломать**.

---

## 0. Почему упало всё, если упал один сервер

Панель Remnawave — не «админка». Это единственный владелец состояния всей системы:
в её Postgres лежат пользователи, подписки, ключи, сквады, ноды, счётчики трафика.
К ней в рантайме обращаются все остальные части:

```
                    ┌─────────────────────────────┐
                    │  Сервер панели (ЕДИНСТВЕННЫЙ)│
                    │  z.fatvdsnvv.space           │
                    │  Remnawave + Postgres        │
                    └──────┬──────────┬───────────┘
             REST API      │          │  управление нодами (:2222)
       ┌───────────────────┤          └──────────────┐
       │                   │                         │
┌──────┴──────┐    ┌───────┴────────┐        ┌───────┴───────┐
│ BFF          │    │ sub-страница   │        │ VPN-ноды      │
│ 87.121.221.229│   │ sub.fatklyuchi │        │ (N штук)      │
│ /config,     │    │ .space         │        │ VLESS/Hysteria│
│ /servers, /me│    │ (Yandex CDN →  │        │               │
└──────┬───────┘    │  origin=панель)│        └───────────────┘
       │            └────────────────┘
┌──────┴───────┐
│ Приложение +  │        Бот ──► BFF ──► панель (та же цепочка)
│ Telegram-бот  │
└───────────────┘
```

Что происходит при отказе панели, по компонентам:

| Компонент | Поведение при мёртвой панели |
|---|---|
| VPN-ноды | **Продолжают работать** на последнем полученном конфиге. Но: новый пользователь на ноду не попадёт, отключённый не отключится, а нода, перезапустившаяся в этот момент, останется без конфига |
| BFF `/config`, `/servers` | 502 (панель — upstream, `Remnawave__BaseUrl: https://z.fatvdsnvv.space`) → приложение не может подключиться и не видит серверов |
| BFF `/trial`, пейринг | Мертвы — создание пользователя и резолв подписки идут в панель |
| Бот (выдача/продление ключей) | Мёртв — вся работа с ключами идёт через панель |
| sub.fatklyuchi.space | Мертва — origin Yandex CDN смотрит на панель |
| Уже подключённые клиенты | Работают, пока не порвётся сессия и не понадобится свежий конфиг |

### 0.1 Разведка живой системы (2026-08-05) — по дашбордам и SSH

Панель, Cloudflare, Selectel и ClouDNS осмотрены через залогиненные дашборды;
87.121.221.229 — по SSH (ключ работает). ~~SSH на сервер панели (95.85.248.65) у
нас НЕТ~~ ✏️ **2026-08-06: доступ получен** — Роберт прислал root-пароль сервера
панели (скриншоты из Telegram; пароль хранится вне git). Аудит §2 и всё, что
упиралось в вопрос 8, разблокировано — см. §0.2.

| Область | Факт |
|---|---|
| Панель | **Remnawave 2.7.4** — линейка v2 → секреты в `.env` это `JWT_AUTH_SECRET`+`JWT_API_TOKENS_SECRET` (`APP_SECRET` — v3+, к нам не относится). 193 пользователя онлайн, ~300 GiB трафика/сутки |
| Ноды | **22 штуки** (16 онлайн, 7 оффлайн, из них Norway/Australia — DANGLING), хостеры разные (1cent, 4vps, arpozan.cloud, hata-internet.space, fatsarvers.pro), версии node-агента вразнобой: 2.7.0 / 2.8.0 / 3.0.0 |
| Бэкап бота | ⚠️ **Уточнение 2026-08-05: 87.121.221.229 — это ТЕСТОВЫЙ бот (@testfatvpnnbot)**; прод-бот живёт на других серверах, доступа к которым у нас нет — всё в этой и следующей строке описывает тестовый инстанс и на прод переносится только как гипотеза (код, скорее всего, тот же, но проверять надо там). Тестовый бот **сам** шлёт свой SQLite `/opt/FatVPN/db.db` в Telegram (`payments_chat`, топик 48) **каждые 4 часа**, 3 ретрая по 30 с. Heartbeat'а нет: три неудачи подряд — строчка в лог и тишина |
| Бэкап BFF | **Нет вообще** (подтверждено: crontab пуст, таймеров нет) — Postgres BFF с аккаунтами/сессиями/триалами не бэкапится никуда |
| Бэкап панели | По словам заказчика — идёт в Telegram; **чем реализован — увидеть нельзя без SSH на сервер панели**. Аудит §2 упирается в этот доступ |
| «Фейловер бота» | На **тестовом** боте не найден: ни watchdog'а, ни скриптов переключения. `core/server_monitor.py` — это мониторинг *нод* (TCP:443, раз в 60 с, алерт в TG с упоминанием админов), не фейловер. ⚠️ Побочная находка: его список HOSTS (3 NL + 3 NO адреса) **не пересекается с текущим списком нод панели** — монитор проверяет старые IP. **Прод-бот и его два сервера с автопереключением мы не видели вовсе** — IP обоих и доступ надо запросить, прежде чем проектировать третий узел |
| DNS `z.fatvdsnvv.space` | A → 95.85.248.65 напрямую, зона на NS регистратора (orderbox) — фейловер невозможен, перенос в CF обязателен (§6.2) |
| `keys.hata-internet.space` | Указывает на **тот же** 95.85.248.65 — второе публичное имя, умирающее вместе с сервером панели. Зона `hata-internet.space` на **reg.ru у владельца панели**, не у нас — при фейловере её запись руками не переведёшь. Выяснить, что на этом имени живёт (страница ключей?) и кто им пользуется |
| `sub.fatklyuchi.space` | CNAME → **Yandex Cloud CDN** (188.72.103.3, AS215013) — не Selectel |
| Selectel | CDN: 4 ресурса, среди них **два подготовленных на `z.fatvdsnvv.space`, которые в DNS-пути сейчас не участвуют** (A-запись идёт мимо CDN) — выяснить у владельца, зачем они. Серверов нет, S3-бакетов нет. Баланс **841 ₽** ⚠️ — на сервер+хранилище не хватит, пополнять |
| Cloudflare | В аккаунте одна зона — `fatklyuchi.space`. Панельного домена нет (сходится с NS-проверкой) |
| ClouDNS | Free-план. Лежит **неавторитетная копия зоны `fatklyuchi.space`** (создана 2026-06-08 — рудимент до переезда в CF). DNS Failover у ClouDNS платный — как запасной вариант §6.3 годится, но не бесплатно |

Отсюда два вывода, определяющие весь план:

1. **Худший сценарий — не простой, а потеря данных.** Бэкапы существуют (поправка
   заказчика: базы панели и бота едут в Telegram), поэтому этап 1 — не «создать»,
   а **проверить**: попадает ли в архив `.env` с секретами (без `APP_SECRET`
   восстановленная база не пустит в панель и ноды её не примут), и разворачивается
   ли архив вообще (§2). Бэкап, который ни разу не восстанавливали, — это надежда,
   а не бэкап.
2. **Резервировать только панель недостаточно.** BFF (`87.121.221.229`) — точно такой
   же единственный сервер без бэкапов, и его отказ кладёт приложение у всех
   пользователей независимо от числа панелей. Это за рамками поставленной задачи,
   но зафиксировано в §11 — закрыть одну дверь из двух не считается.

### 0.2 Данные от Роберта (2026-08-06, скриншоты из Telegram)

| Сервер | Факты |
|---|---|
| **#s13956 — сервер панели** (подтверждено Робертом: «Это от панели», «Отсюда бекапы делаются») | `95.85.248.65`, Нидерланды, Ubuntu 24.04, **2 vCore / 4 GB RAM / 40 GB NVMe**, 500 ₽/мес, статус «активен». **Root-логин и пароль присланы** (хранить в менеджере паролей, в git не класть) — вопрос 8 закрыт |
| **#s13947 — роль НЕ названа** | `95.85.248.29` — та же /24, что панель, т.е. **тот же провайдер**. Ubuntu 24.04, 4 vCore / 8 GB RAM / 100 GB NVMe. Root-пароль тоже прислан. Гипотеза: один из прод-серверов бота (вопрос 4) — **спросить Роберта, что на нём живёт**, прежде чем что-либо на нём делать |

Что из этого следует:

- ⚠️ **Оба сервера оплачены до 16.08.2026** (через ~10 дней). Просроченный сервер
  панели — это ровно повторение инцидента 2026-08-03, только по календарю.
  Убедиться, что продление под контролем, — до любых других работ.
- ⚠️ **Диск панели — 40 ГБ** против требования §1.2 «60+ ГБ, запас ×4 под WAL и
  бэкапы». Репликационные слоты (§4) копят WAL на primary, пока standby отстаёт, —
  на 40 ГБ это заметный риск переполнения. При аудите снять фактический размер
  базы и свободное место; возможно, primary после учений стоит переехать на
  сервер покрупнее (система симметрична, «главного» нет — §7.2).
- **Бэкапы панели видны в Telegram воочию**: `remnawave_scheduled_YYYYMMDD_HHMMSS.tar.gz`
  ~38.6 МБ (видны `120003` и `180003` — шаг **6 часов**), рядом `db.db` 9.3–9.4 МБ
  (SQLite бота). Имя файла указывает на готовый планировщик
  (remnawave-backup-restore-подобный). Что внутри tar.gz (`.env`? compose?) — теперь
  проверяемо по SSH; 38.6 МБ уже близко к лимиту `sendDocument` в 50 МБ — рост базы
  молча упрёт бэкап в лимит (типовой отказ №2 из §2).
- Ориентир цены из §1.2 (€8–20/мес) завышен против фактических 500 ₽ (~€5) — бюджет
  вопроса 6 скорее скромнее, чем закладывали.
- Если #s13947 задумывался как «резерв панели» — для роли B он **не годится по
  требованию §1.2**: тот же провайдер и та же подсеть, что у A (общий отказ ДЦ
  снимает оба). Годится под другие роли (например, узел бота).

---

## 1. Почему «три работающих панели» невозможны, и что возможно вместо них

У Remnawave нет кластерного режима. Две одновременно **активные** панели — это два
независимых Postgres, в которых состояние расходится с первой же операции: ключ
выдан в одной базе, продление записано в другой, счётчики трафика различаются
навсегда. Обратной процедуры слияния не существует.

Поэтому «три сервера» реализуются единственным корректным способом:

**Active / passive: один активный + два горячих резерва.**

- Панель (веб + API) запущена **только на активном** сервере.
- Postgres работает на всех трёх: primary на активном, два standby непрерывно
  догоняют его потоковой репликацией (отставание — секунды).
- При отказе активного: трафик переводится на резерв (Cloudflare Load Balancer,
  автоматически), standby промоутится в primary и на нём запускается панель
  (полуавтоматически, см. §7 — и это осознанное решение, а не недоделка).

### 1.1 Отвергнутые альтернативы — чтобы к ним не возвращаться

| Вариант | Почему нет |
|---|---|
| Active/active (3 живых панели) | Нет кластера → расходящиеся базы, несводимо. Не «сложно», а «нельзя» |
| keepalived / плавающий IP | Требует одного провайдера (floating IP не переезжает между хостерами) → отказ ДЦ или блокировка провайдера снимает все три сервера разом. Это воспроизведение вчерашней аварии за тройную цену |
| Периодический rsync/дамп на резерв вместо репликации | Резерв со «вчерашним» состоянием: всё, что куплено/продлено после последнего дампа, исчезает при переключении. Дампы остаются, но как слой бэкапа, не как механизм HA |
| Полный автопромоушен БД со своим скриптом | Split-brain: сеть между A и наблюдателем моргнула → поднялись два primary → расхождение хуже простоя. Автопромоушен возможен только с кворумом (Patroni/repmgr) — это этап 2, после того как базовая схема переживёт учения (§7.3) |

### 1.2 Целевая схема

```
                Cloudflare Load Balancer  (z.fatvdsnvv.space, DNS-only, TTL 30-60с)
                health-check GET / каждые 15с из нескольких регионов
                        │
        приоритет 1 ────┼──── приоритет 2 ──────── приоритет 3
        ┌───────────────┴──┐  ┌──────────────────┐  ┌──────────────────┐
        │ Сервер A (акт.)   │  │ Сервер B (резерв)│  │ Сервер C (резерв)│
        │ провайдер 1       │  │ провайдер 2      │  │ провайдер 3      │
        │ Remnawave: RUN    │  │ Remnawave: STOP  │  │ Remnawave: STOP  │
        │ Postgres: primary │  │ Postgres: standby│  │ Postgres: standby│
        └────────┬──────────┘  └────────┬─────────┘  └────────┬─────────┘
                 │      WireGuard-меш (10.77.0.0/24)          │
                 └────────── streaming replication ───────────┘
                             A → B  и  A → C

        Ноды: firewall 2222/tcp открыт для ВСЕХ ТРЁХ публичных IP заранее
```

Три сервера — **у трёх разных провайдеров** (или минимум у двух), в Европе, близко
по RTT. Одна локация дала бы приватную сеть и мгновенный floating IP, но общий
отказ ДЦ снимает все три; DNS-переключение за 30–60 секунд — приемлемая цена за
независимость. Репликация между провайдерами идёт по открытому интернету → только
через WireGuard.

Требования к каждому серверу: 2–4 vCPU, 4–8 ГБ RAM, 60+ ГБ NVMe, Ubuntu 22.04/24.04.
Панель нетребовательна; диск должен вмещать базу с запасом ×4 (WAL + бэкапы).
Ориентир цены: €8–20/мес за сервер.

---

## 2. Этап 1 — аудит существующих бэкапов. Делается первым, до покупки серверов

✏️ **Переписано 2026-08-04 после поправки заказчика: бэкапы есть** (панель и бот →
Telegram). Раздел из «создать» превратился в «проверить и при необходимости
дополнить». Проверка — не формальность: бэкап считается существующим только после
успешного восстановления, и известны три типовых способа, которыми «бэкап в
Telegram» оказывается пустышкой ровно в день аварии:

1. **В архиве нет `.env`.** Дамп базы без `APP_SECRET` (v3; для v2 —
   `JWT_AUTH_SECRET`+`JWT_API_TOKENS_SECRET`) восстанавливается в панель, которая
   отдаёт 403 на логин и которую не принимают ноды. Данные есть — доступа нет.
2. **Бэкап молча умер N недель назад** (кончилось место, протух токен бота, архив
   перерос лимит Telegram в 50 МБ), и никто не заметил, потому что отсутствие
   сообщения не алертит. Проверить дату последнего архива и поставить heartbeat
   (§2.2, шаг 5).
3. **Архив никто никогда не разворачивал** — и в день аварии выясняется, что дамп
   делался не тем инструментом/не с теми флагами, восстановлению мешает версия
   Postgres, или в архиве только часть каталога панели.

Чек-лист аудита (по SSH на сервере панели; готовые команды под каждый пункт —
`docs/panel-backup-audit-runbook.md`, составлен 2026-08-06):

- [x] ~~найти механизм~~ **Найден и прочитан (2026-08-06, аудит по SSH —
      результаты в `panel-backup-audit-runbook.md`)**: `remnawave.sh` v6.1.7
      (DigneZzZ/remnawave-scripts), cron каждые 6 ч. Архив — полный restore-kit:
      `database.sql`, `.env` **с обоими v2-секретами** ✅, compose с запиненной
      версией панели, Caddyfile, `RESTORE-INSTRUCTIONS.md`;
- [x] ~~частота и дата последнего~~ **Каждые 6 часов, идёт по расписанию — но
      найдена дыра 29.07→04.08 (~5.5 суток), молча накрывшая инцидент 2026-08-03**:
      cron просто не запускался, в логе нет даже «Starting», причина не
      установлена. Это случившийся в реальности «типовой отказ №2» — довод, почему
      heartbeat (след. пункт) обязателен;
- [ ] куда, кроме Telegram: единственное назначение — это единственная точка отказа
      хранилища; добавить вторую копию в Selectel S3 (`rclone`, §2.2) — доступ есть;
- [x] ~~heartbeat-мониторинг~~ **Сделано 2026-08-06, взаимными сторожами вместо
      healthchecks.io** (внешних аккаунтов не потребовалось): панель и BFF каждый
      час проверяют по SSH свежесть бэкапов **друг друга** и алертят в TG-топик
      бэкапов при устаревании (панель: порог 7 ч; BFF: 2 ч), отсутствии архивов
      или недоступности соседа. `send_backup.sh` панели усилен: ретраи ×3, алерт
      при несвежем архиве, cron `&&`→`;` (упавший `remnawave backup` больше не
      отменяет проверку). Тестовый алерт доставлен. Остаточная слепая зона: оба
      сервера мертвы одновременно — тогда не алертит никто (закроется CF LB
      monitor'ом на этапе 5 или внешним healthchecks.io);
- [ ] **тестовое восстановление на чистой VM** (§2.3) — главный пункт, остальные
      обслуживают его. Заодно даёт замер времени, который ляжет в ранбук §7;
- [x] ~~найти механизм бэкапа **бота**~~ — найден (2026-08-05): бот сам шлёт
      `db.db` в TG каждые 4 ч, 3 ретрая, **без heartbeat** — добавить пинг
      healthchecks.io после успешной отправки и вторую копию в S3;
- [x] ~~для **BFF** бэкапов **нет**~~ **Заведены 2026-08-06**
      (`/opt/fatvpn-ops/backup_bff.sh`, cron `:17` ежечасно): `pg_dumpall`
      fatvpn-postgres + конфиги стека (`.env`, compose, Caddyfile) + состояние
      бота (`db.db`) → `/opt/fatvpn-backups`, ретеншен 48 ч, каждые 6 ч копия
      в тот же TG-топик, алерты при сбое. Первый прогон прошёл (дамп 108 КБ).
      ⚠️ Бот на этом сервере — тестовый; прод-бот бэкапится своей схемой (§10);
- [x] бэкап **панели**: ✏️ 2026-08-06 аудит по SSH **проведён**
      (`panel-backup-audit-runbook.md`): архив вскрыт, `.env` с
      `JWT_AUTH_SECRET`/`JWT_API_TOKENS_SECRET` внутри ✅, дамп — plain
      `database.sql`. Осталось из этапа 1: **тестовое восстановление §2.3**,
      heartbeat, вторая копия в S3 (отправка сейчас — только TG, без ретраев,
      шлёт только последний архив).

### 2.1 Что должно оказаться в архиве (эталон для аудита)

| Что | Зачем |
|---|---|
| `pg_dump` базы панели (контейнер `remnawave-db`) | Всё состояние: пользователи, ключи, подписки, ноды |
| `/opt/remnawave/.env` (или где лежит стек панели) | **Критично:** `APP_SECRET` (panel v3+; для v2 — `JWT_AUTH_SECRET` + `JWT_API_TOKENS_SECRET`). Восстановленная база с чужим секретом отдаёт 403 на логин и не аутентифицируется — база есть, войти нельзя |
| `docker-compose.yml` панели и всё рядом (конфиги Xray, кастомные шаблоны) | Чтобы восстановление было «развернуть архив», а не «вспомнить, как было настроено» |

### 2.2 Куда и как

Два независимых назначения — хранилище может быть недоступно ровно тогда, когда
нужен бэкап:

1. **S3-совместимое хранилище** через `rclone`. Доступ к **Selectel** уже есть — его
   объектное хранилище подходит (панель стоит не у Selectel, требование «не у того же
   провайдера» соблюдается; если один из серверов B/C возьмём у Selectel — бэкапы это
   не ломает, у них останутся ещё две копии). Retention: 24 почасовых, 30 суточных.
2. **Telegram** — документом в приватный канал (`sendDocument`, лимит 50 МБ — дамп
   в `gzip -9` в него влезает с большим запасом; когда перестанет влезать, в
   Telegram остаётся только суточный, почасовые — в S3).

Скрипт `/opt/remnawave/backup.sh` (cron: `7 * * * *` — не в :00, чтобы не совпадать
с чужими пиками):

```bash
#!/usr/bin/env bash
set -euo pipefail
STAMP=$(date -u +%Y%m%d-%H%M%S)
DIR=/opt/remnawave-backups
mkdir -p "$DIR"

# 1. Дамп базы. Имя контейнера БД проверить на месте: docker ps | grep db
docker exec remnawave-db pg_dumpall -U postgres | gzip -9 > "$DIR/db-$STAMP.sql.gz"

# 2. Конфиги и секреты стека
tar czf "$DIR/stack-$STAMP.tar.gz" -C /opt remnawave/.env remnawave/docker-compose.yml

# 3. S3 (rclone remote 'b2' настроить заранее: rclone config)
rclone copy "$DIR/db-$STAMP.sql.gz"    b2:fatvpn-panel-backups/hourly/
rclone copy "$DIR/stack-$STAMP.tar.gz" b2:fatvpn-panel-backups/hourly/

# 4. Telegram (свой бот-канал, НЕ рабочий бот)
curl -sf -F chat_id="$TG_CHAT" -F document=@"$DIR/db-$STAMP.sql.gz" \
  "https://api.telegram.org/bot$TG_TOKEN/sendDocument" > /dev/null

# 5. Ротация локальных копий и heartbeat: если скрипт молчит час — алерт
find "$DIR" -mtime +2 -delete
curl -sf "https://hc-ping.com/<uuid>" > /dev/null   # healthchecks.io, бесплатный
```

Шаг 5 обязателен: **бэкап, который молча перестал делаться, хуже отсутствующего** —
он создаёт ложную уверенность. healthchecks.io шлёт алерт в Telegram, если пинг не
пришёл вовремя.

Готовая альтернатива скрипту — [remnawave-backup-restore](https://github.com/distillium/remnawave-backup-restore)
(distillium): то же самое (директория + БД, расписание, Telegram, S3/GDrive,
retention) в готовом виде. Приемлемо; собственный скрипт прозрачнее на 30 строк.
Что бы ни выбрали — heartbeat-мониторинг поверх обязателен.

### 2.3 Тестовое восстановление — часть этапа, не «потом»

На чистой VM (можно временной, за €0.01/час): развернуть панель с нуля → залить
дамп → скопировать `.env` из бэкапа → войти в панель → увидеть пользователей и ноды.
Замерить время. Это репетиция ровно вчерашнего сценария.

**Готово, когда:**
- [ ] почасовой бэкап лежит в S3 **и** в Telegram, retention работает — ⏳ S3 ждёт
      ключей Selectel (баланс 841 ₽, пополнить); Telegram и retention работают;
- [x] ~~healthchecks.io~~ алертинг при пропуске — **взаимные сторожа панель↔BFF
      (2026-08-06)**, тестовый алерт доставлен; прогон «сломать на час» — на учениях;
- [x] восстановление прошло **(2026-08-06, ~4 мин 30 с — против ориентира 30 мин)**:
      изолированный Docker-стенд на BFF (`internal: true` — обязательно: панель
      с боевой базой немедленно ломилась ко всем нодам на 2222, изоляция сдержала),
      дамп лёг без единой ошибки, панель healthy, **6100 пользователей / 23 ноды /
      1 админ** на месте;
- [x] в бэкапе есть `.env` ✅; логин: аутентификация работает (API отвечает, 403
      на неверные креды), но ⚠️ **`admin-credentials.txt` в архиве устарел** —
      username в живой базе другой, актуальные креды только у Роберта. Вход его
      кредами проверить на учении У2; сказать Роберту, что файл в архиве стар.

---

## 3. Этап 2 — серверы и сеть

### 3.1 Покупка

B и C — у **других** провайдеров, не у того, где A. Спецификация из §1.2. При выборе
проверить: провайдер не блокирует UDP 51820 (WireGuard) и даёт rescue/консоль
(пригодится для fencing, §7.1).

### 3.2 WireGuard-меш

Полный меш из трёх узлов, подсеть `10.77.0.0/24`: A=`10.77.0.1`, B=`.2`, C=`.3`.
`PersistentKeepalive = 25` (NAT здесь нет, но узлы за строгими firewall).
Репликация Postgres и всё межсерверное — **только** по этим адресам; Postgres на
публичных интерфейсах не слушает вообще.

Firewall каждого сервера панели: наружу открыты 80/443 (+SSH со своих IP), 51820/udp
для двух других узлов; порт Postgres — только на WG-интерфейсе.

**Готово, когда:**
- [ ] `ping 10.77.0.x` ходит между всеми парами, переживает перезагрузку узла;
- [ ] `pg_isready -h 10.77.0.1` с B и C — ок; с публичного IP порт Postgres закрыт
      (проверить nmap'ом снаружи).

---

## 4. Этап 3 — репликация Postgres

Потоковая физическая репликация, primary A → standby B и C, с репликационными
слотами (слот гарантирует, что primary не выкинет WAL, пока отставший standby его
не забрал — цена: следить за диском, см. мониторинг §9).

### 4.1 На A (primary)

В конфиг Postgres контейнера панели (`postgresql.conf` или command-флаги compose):

```
wal_level = replica          # дефолт в PG 14+, проверить
max_wal_senders = 5
max_replication_slots = 5
wal_keep_size = 1GB
```

`pg_hba.conf` — только WG-адреса:

```
host  replication  replicator  10.77.0.2/32  scram-sha-256
host  replication  replicator  10.77.0.3/32  scram-sha-256
```

Пользователь: `CREATE ROLE replicator WITH REPLICATION LOGIN PASSWORD '<...>';`
Слоты: `SELECT pg_create_physical_replication_slot('standby_b');` (и `standby_c`).

Порт наружу из контейнера — публиковать **только на WG-адрес**:
`"10.77.0.1:5432:5432"` в compose панели.

### 4.2 На B и C (standby)

```bash
docker compose stop remnawave-db   # и панель там вообще не запущена
docker run --rm -v remnawave-db-data:/var/lib/postgresql/data postgres:16 bash -c \
  'rm -rf /var/lib/postgresql/data/* && \
   pg_basebackup -h 10.77.0.1 -U replicator -D /var/lib/postgresql/data \
     -X stream -R --slot=standby_b -C'
docker compose start remnawave-db
```

`-R` пишет `standby.signal` + `primary_conninfo` сам. На C — то же со `standby_c`.
Имя тома смотреть в компоузе панели на месте.

⚠️ Мажорная версия Postgres на всех трёх — **одна и та же, до минора не критично,
до мажора обязательно**. Физическая репликация между разными мажорами не работает.
Зафиксировать версию тегом образа в compose, не `latest`.

### 4.3 Проверка

На A: `SELECT client_addr, state, replay_lag FROM pg_stat_replication;` — две строки
`streaming`, lag < 5 с. На B: создать тестового пользователя в панели на A и увидеть
его строку в standby-базе через `psql` за секунды.

**Готово, когда:**
- [ ] оба standby в `streaming`, replay_lag < 5 с под обычной нагрузкой;
- [ ] тестовая запись на A видна на B и C в течение 10 с;
- [ ] рестарт Postgres на B → сам вернулся в `streaming` без рук;
- [ ] обрыв WG-туннеля на 10 минут → standby догнал после восстановления (слот
      удержал WAL), диск на A это пережил.

---

## 5. Этап 4 — три идентичные панели

Резерв обязан быть **бинарно и конфигурационно идентичен** активному, иначе в момент
аварии вместо переключения будет отладка.

| Что | Требование | Что будет, если нет |
|---|---|---|
| Версия образа Remnawave | Одинаковый тег на всех трёх; обновление панели = обновление всех трёх в один день (сначала B/C — их простой ничего не стоит, потом A) | Промоушен на старую версию → миграции «из будущего» в базе, панель не стартует |
| `APP_SECRET` / JWT-секреты в `.env` | **Побайтно одинаковые** | 403 на логин; ноды не принимают панель; все выданные токены невалидны |
| TLS-сертификат `z.fatvdsnvv.space` | Выпущен **заранее на всех трёх** через **DNS-01** (acme.sh / certbot с Cloudflare API-токеном), авто-продление на каждом | HTTP-01 на пассивном узле не пройдёт никогда (домен смотрит на A) → резерв поднимается без валидного серта → приложение и бот получают TLS-ошибку вместо панели |
| compose/конфиги | В git (приватный repo) или синхронизированы скриптом; руками по трём серверам — не вариант | Дрейф конфигурации, всплывающий только при аварии |
| Панель на B и C | **Остановлена**, автостарт выключен (`restart: "no"` на пассивных или `systemctl disable` враппера) | Резерв, сам стартующий при перезагрузке, — готовый второй primary и split-brain |

### 5.1 Публичные имена, завязанные на сервер панели (разведано 2026-08-05)

Фейловер должен уводить с мёртвого сервера **все** имена, а их оказалось больше
одного:

| Имя | Сейчас | Что сделать |
|---|---|---|
| `z.fatvdsnvv.space` | A → 95.85.248.65 напрямую, зона у orderbox | Основной кейс: перенос зоны в CF + LB (§6.2) |
| `keys.hata-internet.space` | A → **тот же** 95.85.248.65; зона на reg.ru **у владельца панели** | Выяснить, что на нём живёт и кто пользуется. Варианты: перевесить CNAME'ом на `z.fatvdsnvv.space` (тогда LB покрывает и его), либо признать, что оно умирает вместе с A и это приемлемо. Оставить как есть молча — нельзя |
| `sub.fatklyuchi.space` | CNAME → Yandex Cloud CDN | Проверить **origin** этого CDN-ресурса: если там IP сервера панели — сменить на домен, чтобы фейловер чинил и страницу подписки |
| Selectel CDN ×2 на `z.fatvdsnvv.space` | Созданы, но в DNS-пути не участвуют | Спросить владельца, зачем; в схеме LB им места нет — либо удалить, либо задокументировать |

### 5.2 BFF и бот при фейловере

Ничего менять не нужно — и это надо **сохранить**: BFF ходит в панель по
`https://z.fatvdsnvv.space` (домен, не IP), бот — через BFF. Правило на будущее:
**ни один клиент панели никогда не конфигурируется её IP-адресом.** Появится соблазн
«захардкодить, чтобы не зависеть от DNS» — он ломает весь фейловер.

**Готово, когда:**
- [ ] `diff .env` между A/B/C — расходится только hostname-специфика (в идеале ничего);
- [ ] на B и C лежит валидный серт `z.fatvdsnvv.space`, `openssl x509 -enddate` > 30 дней,
      авто-продление в кроне отработало хотя бы раз (форсировать `--force-renewal`);
- [ ] панель на B, запущенная руками в тестовом окне со своим локальным Postgres
      (промоушен копии), пускает в админку и показывает данные;
- [ ] автостарт панели на B и C выключен, перезагрузка сервера её не поднимает.

---

## 6. Этап 5 — ноды и Cloudflare Load Balancer

### 6.1 Firewall нод — заранее, на каждой

Панель управляет нодой по `NODE_PORT` (2222/tcp), и порт должен быть открыт только
для панели. Значит **на каждой ноде — все три** публичных IP:

```bash
ufw allow from <IP_A> to any port 2222 proto tcp
ufw allow from <IP_B> to any port 2222 proto tcp
ufw allow from <IP_C> to any port 2222 proto tcp
```

Без этого резервная панель поднимется, покажет админку — и не сможет управлять ни
одной нодой. Это обнаружилось бы ровно в момент аварии. Новая нода в будущем =
сразу три правила (внести в процедуру добавления ноды).

### 6.2 Cloudflare Load Balancer

Требование: зона `fatvdsnvv.space` должна обслуживаться в Cloudflare. **Проверено
2026-08-04: сейчас она НЕ там** — NS на дефолтных серверах регистратора
(`*.orderbox-dns.com`), фейловера у них нет. Аккаунт CF есть (тот же, где
`fatklyuchi.space`), так что первый шаг этапа — перенос зоны: добавить в CF,
скопировать **все** записи один-в-один (не только `z.` — в зоне могут жить имена
нод), у регистратора сменить NS. С заранее скопированными записями простоя нет;
все записи — **серое облако**, как и во второй зоне.

Настройка (Traffic → Load Balancing):

- **Monitor**: HTTPS GET `/` (или путь логин-страницы панели), интервал 15 с,
  timeout 5 с, retries 2, ожидаемый код 200–399, проверка из 2–3 регионов.
- **Pools** (по одному origin в каждом): `panel-a`, `panel-b`, `panel-c`.
- **Load Balancer** на hostname `z.fatvdsnvv.space`: режим **failover** — порядок
  пулов A → B → C; **Proxy OFF (DNS-only, серое облако)**, TTL 30 с.
- Notifications → алерт в Telegram/почту при смене здоровья пула.

DNS-only здесь — осознанный выбор: LB меняет DNS-ответ на здоровый origin, оранжевое
облако не участвует. Правило проекта «серые облака, и только серые» (оранжевое убило
ноды единственный раз, когда его включили) остаётся в силе без исключений — так его
не надо помнить с оговорками. Цена: $5–15/мес (2 origin включены в базовые $5,
третий — доплата).

⚠️ Что LB **делает и чего не делает**: он переводит **трафик** на B за ~30–60 с
(health-check + TTL). Он **не** промоутит базу и **не** запускает панель — это §7.
До промоушена B отвечает connection refused, что для клиентов неотличимо от простоя.
Поэтому время фейловера системы = время реакции человека на алерт + 5 минут ранбука,
а не «30 секунд» (пока не сделан этап-2 автопромоушен, §7.3).

### 6.3 Если Cloudflare для этой зоны недоступен

Первый запасной вариант — **ClouDNS** (доступ уже есть): у них managed DNS Failover
с health-check'ами — перенос зоны туда вместо CF даёт тот же механизм. Выбираем CF
только потому, что вторая зона проекта уже там и экосистема одна.

Второй запасной — свой наблюдатель (по образцу того, что уже работает у бота):
скрипт на **независимом** от всех трёх серверов узле (например, сервер BFF), проверка
`https://z.fatvdsnvv.space/` каждые 30 с, при N подряд отказов — перепись A-записи
через API DNS-провайдера (TTL заранее 60 с) + алерт. Хуже CF LB ровно тем, что
наблюдатель — наш код и наша новая точка отказа; выбирать только при недоступности LB.

**Готово, когда:**
- [ ] на каждой ноде `nc -zv <нода> 2222` проходит с A, B и C (проверить с самих серверов);
- [ ] LB создан, monitor «зелёный» на A, «красный» на B/C (панель там и должна лежать) —
      и режим failover при этом держит DNS на A (проверить `dig z.fatvdsnvv.space`);
- [ ] алерт о смене здоровья пула приходит в Telegram (проверено остановкой панели на A
      в тестовое окно);
- [ ] `dig` с TTL ≤ 60.

---

## 7. Этап 6 — переключение (промоушен) и возврат

### 7.1 Ранбук фейловера — одна страница, отрепетированная

Печатается и кладётся туда, где его найдут в 3 часа ночи. Порядок жёсткий:

```
0. Убедиться, что A действительно мёртв, а не «медленный»:
   - панель не отвечает С ДВУХ разных сетей;
   - SSH на A не ходит / провайдер показывает down.
   Если A жив, но деградировал — сначала лечить A, промоушен не делать.

1. FENCING — отсечь A, чтобы он не вернулся primary'м самовольно:
   - если есть доступ: на A `docker compose down` (панель + БД) и запретить автостарт;
   - если доступа нет: выключить сервер через панель провайдера (power off);
   - НЕЛЬЗЯ продолжать, пока не сделано одно из двух. Вернувшийся A с живой
     панелью = split-brain, авария хуже простоя.

2. На B промоутить Postgres:
   docker exec remnawave-db psql -U postgres -c "SELECT pg_promote();"
   → проверить: SELECT pg_is_in_recovery();  -- must be false

3. На B запустить панель:
   docker compose up -d
   → открыть https://z.fatvdsnvv.space (DNS уже переведён LB'ом), войти,
     убедиться, что ноды в панели зелёные (это заодно проверка §6.1).

4. Перевести репликацию C на нового primary B:
   на C: ALTER SYSTEM SET primary_conninfo = 'host=10.77.0.2 ...';
         создать слот на B, рестарт Postgres C, проверить pg_stat_replication на B.
   Некритично для восстановления сервиса — можно через час, но не «завтра»:
   до этого C отстаёт и не является резервом.

5. Сообщить в чат: что, когда, кто промоутил. Бэкап-cron включить на B (§7.4).
```

Пункты 2–3 оформить скриптом `promote.sh` на B и C — но **запускаемым руками**.
Автоматика здесь — этап 2 (§7.3).

### 7.2 Возврат (failback) — без спешки

Пока жив B, система полноценна (C — его standby). A возвращается **как standby**:
чистый `pg_basebackup` с B (старые данные A выбрасываются — после промоушена B их
таймлайны разошлись), затем в спокойное окно — обратный промоушен по тому же
ранбуку, если вообще нужен: система симметрична, «главного» сервера нет.

### 7.3 Этап 2 (после успешных учений): автопромоушен

Если заказчику нужно строго «само, без человека» — Patroni (или repmgr) поверх тех
же трёх Postgres: три узла дают кворум, split-brain решается протоколом, промоушен
за секунды; поверх — хук, поднимающий панель на новом primary. Это существенный
объём (свой etcd/consul или встроенный DCS, обвязка в контейнерах панели), и ставить
его надо **на работающую и отрепетированную** ручную схему, а не вместо неё.
Решение — после учений §8, с замером: если ручной фейловер укладывается в 10–15
минут с алертом в Telegram, возможно, этого достаточно.

### 7.4 Бэкапы при фейловере

Cron бэкапа (§2) стоит на всех трёх, но активен только на текущем primary (шаг
скрипта: `pg_is_in_recovery()` → standby молча выходит, **и не пингует heartbeat** —
пинг с standby маскировал бы умерший бэкап на primary). Проверка «бэкап едет с B»
входит в учения.

---

## 8. Этап 7 — учения. Без них всей схемы нет

Каждое учение — с секундомером и записью результата в этот документ. Проводить в
окно минимальной нагрузки, с предупреждением заказчика.

| # | Сценарий | Что делаем | Успех |
|---|---|---|---|
| У1 | Восстановление из бэкапа (репетиция вчерашнего) | Чистая VM + дамп из S3 + `.env` из бэкапа | Логин работает, данные на месте, время < 30 мин. ✅ **Пройдено 2026-08-06 (~4.5 мин)** в изолированном Docker на BFF: данные на месте (6100/23/1), панель healthy; остаток — вход кредами Роберта (файл в архиве устарел) и повтор из S3, когда S3 появится |
| У2 | Мягкий отказ | На A: `docker compose stop` панели | Алерт CF < 2 мин; DNS уехал на B; ранбук §7.1 выполнен; система жива; общее время записано |
| У3 | Жёсткий отказ (вчерашний сценарий) | Power off сервера A через панель провайдера, без подготовки | То же. Отдельно проверить: ноды остались зелёными в панели на B; приложение подключается; бот выдаёт ключ; sub-страница открывается |
| У4 | Возврат | A обратно как standby, затем обратный промоушен в окно | Оба перехода без потери данных (тест-пользователь, созданный «при B», виден после возврата на A) |
| У5 | Отказ второго | При активном B — power off B | Система уезжает на C. Это и есть заявленное «второй умер — работает третий» |
| У6 | Бэкап с резерва | Во время У3/У5 | Почасовой бэкап продолжил приезжать в S3 с нового primary; heartbeat не рвался |

Повторять У2 (минимум) — раз в квартал: фейловер, который год не трогали, не работает.

---

## 9. Мониторинг и алертинг

Минимальный набор, без него схема слепа:

| Что | Чем | Алерт при |
|---|---|---|
| Доступность панели снаружи | CF LB monitor (уже есть, §6.2) + независимый Uptime Kuma / UptimeRobot | недоступности > 1 мин |
| Лаг репликации | Скрипт на A: `pg_stat_replication.replay_lag`, cron 1 мин → Telegram | lag > 60 с или standby пропал из списка |
| Диск на A | `df` в том же скрипте (слоты копят WAL при мёртвом standby!) | > 80% |
| Бэкапы | healthchecks.io (§2.2) | пропуск часа |
| Сертификаты на B и C | `openssl x509 -enddate` в cron | < 21 дня до истечения |

Uptime Kuma можно поставить на сервер BFF (независим от всех трёх панельных) — либо
использовать внешний UptimeRobot, чтобы не зависеть и от BFF.

---

## 10. Бот: третий резервный сервер

У бота, по словам заказчика, есть основной + запасной с автопроверкой и
переключением. ⚠️ **Уточнение 2026-08-05: наш доступ (87.121.221.229) — это
ТЕСТОВЫЙ бот; прод-бот и оба его сервера мы не видели**, доступа к ним нет — IP
обоих и SSH надо запросить до любого проектирования. Что дала разведка тестового
инстанса (переносится на прод как гипотеза, код вероятно общий): фейловер-механизма
в самом боте нет — единственный кандидат, `core/server_monitor.py`, оказался
мониторингом нод (и смотрит на устаревший список IP — см. §0.1), значит
автопроверка прода реализована чем-то внешним на его серверах. **Состояние бота —
локальный SQLite `db.db`** (уезжает в Telegram раз в 4 часа): резервный бот без
свежей копии этого файла поднимается с данными до 4 часов давности — как прод
решает эту проблему, главный вопрос к его схеме. Дальше — принцип и вопросы, а не
конфиги:

1. **Изучить существующий механизм** прежде, чем строить третий узел: что проверяет
   (getUpdates? свой health?), кто проверяет (внешний наблюдатель? сами узлы друг
   друга?), как передаётся роль (у Telegram-ботов это обычно «кто держит polling» —
   два одновременно живых polling'а на один токен вызывают конфликт 409, что удобно:
   сам Telegram выступает арбитром).
2. **Третий узел — копией второго**, включённой в ту же цепочку проверки: A умер →
   B, B умер → C. Если текущая схема — «B проверяет A», её надо превратить в кольцо
   или вынести наблюдателя, иначе C не узнает о смерти B.
3. **Состояние бота.** Критичный вопрос: что бот хранит локально (SQLite? файлы?
   свой Postgres?) и как это попадает на резервы. Если резервный бот поднимается с
   пустой/старой базой — переключение «работает», но данные (незавершённые оплаты,
   состояния диалогов) теряются. Ответ определяет, нужна ли боту репликация или
   достаточно того, что его истинное состояние живёт в панели и BFF.
4. Бэкапы бота — в ту же систему §2 (S3 + Telegram + heartbeat), со всех узлов.

**Готово, когда:** третий узел в цепочке; учение «выключили A и B — бот отвечает
с C» пройдено; состояние при переключении не теряется (или письменно
зафиксировано, чем жертвуем).

---

## 11. За рамками задачи, но обязано быть сказано: BFF

`87.121.221.229` — BFF + его Postgres (аккаунты, сессии, refresh-токены, триалы,
слоты устройств) + Caddy + тестовый бот. Один сервер. ~~Бэкапов нет.~~ Его отказ =
приложение не работает ни у кого; потеря его диска = все залогинены заново в лучшем
случае, потеря учёта триалов и слотов — в худшем.

✏️ **2026-08-06: минимум сделан** — почасовой `pg_dumpall` + конфиги + `db.db`
тестового бота в `/opt/fatvpn-backups` (ретеншен 48 ч), копия в TG каждые 6 ч,
взаимный сторож с панелью (§2). S3 ждёт ключей Selectel. Попутная находка: BFF
стоит у **того же провайдера, что панель** (`mad-spence.1cent.network` /
`lonely-pare.1cent.network`) — отказ 1cent кладёт и панель, и BFF разом; довод
разнести при постройке резерва BFF.

Полноценный резерв BFF (второй сервер, реплика, тот же CF LB на
`api.fatklyuchi.space` — зона **уже в Cloudflare**, что упрощает) — отдельная задача
после панели. Одно предостережение на тот день: приложение пинит **CA** (ISRG), не
IP и не ключ сервера, так что резервный BFF с Let's Encrypt-сертом пиннинг проходит —
но серт на резерве должен быть выпущен так же заранее и так же через DNS-01 (§5).

---

## 12. Открытые вопросы — блокируют соответствующие этапы

| # | Вопрос | Блокирует |
|---|---|---|
| 1 | ~~Зона `fatvdsnvv.space` — в Cloudflare?~~ **Отвечено 2026-08-04: НЕТ** — NS зоны сидят на дефолтных серверах регистратора (`*.orderbox-dns.com`), у которых нет ни health-check'ов, ни фейловера. Аккаунт Cloudflare **есть** (там же, где `fatklyuchi.space`), доступы к Selectel и ClouDNS — тоже. → Новый шаг этапа 5: **перенести зону в CF** (добавить зону, скопировать все записи один-в-один, сменить NS у регистратора — та же операция, что уже проделана с `fatklyuchi.space`; при заранее скопированных записях простоя не даёт). Остаток вопроса: выпуск API-токена на зону для DNS-01 (§5) | §6.2 (LB) и DNS-01 (§5) |
| 2 | Где стоит текущий сервер панели — **отвечено 2026-08-04: OC Networks Ltd (AS209693), Амстердам, `95.85.248.65`**. → B и C — у двух других провайдеров; один может быть Selectel (доступ есть), второй — любой третий. ~~Остаток: ресурсы~~ **Отвечено 2026-08-06 (§0.2): 2 vCore / 4 GB RAM / 40 GB NVMe, 500 ₽/мес** — диск меньше спека §1.2, см. предупреждение в §0.2 | §3.1 (выбор B и C) |
| 3 | Сколько нод — **отвечено 2026-08-05: 22** (16 онлайн / 7 оффлайн, см. §0.1). Остаток вопроса: у кого root на них (имена нод разбросаны по ≥5 хостерам) | §6.1 |
| 4 | Как устроен текущий фейловер бота — **переформулировано 2026-08-05**: наш сервер (87.121.221.229) оказался **тестовым ботом**; прод-бот и оба его сервера не видены вовсе. Нужны: **IP основного и запасного прод-серверов бота + SSH на оба**. ✏️ 2026-08-06: возможный кандидат — сервер **#s13947, `95.85.248.29`** (§0.2), root-пароль есть, но Роберт его роль не назвал — **спросить**, прежде чем заходить | §10 целиком |
| 5 | Версия Remnawave — **отвечено 2026-08-05: 2.7.4 (v2)** → секреты `JWT_AUTH_SECRET`+`JWT_API_TOKENS_SECRET`. ~~Остаток: версия Postgres~~ **Закрыт 2026-08-06 (аудит): PostgreSQL 17.6, запинен тегом в compose ✅; `wal_level=replica` уже стоит** → B и C ставить на `postgres:17.6`, рестарт primary для §4 не понадобится | ~~§2.1, §4~~ ничего |
| 8 | ~~**SSH-доступ на сервер панели (95.85.248.65)**~~ **Закрыт 2026-08-06**: Роберт прислал root-пароль (и панели, и второго сервера 95.85.248.29 — см. §0.2; пароли вне git). §2 разблокирован полностью | ~~§2, §4, §5~~ ничего |
| 9 | `keys.hata-internet.space` — что на нём живёт, кто пользуется, и почему зона у владельца панели на reg.ru (§5.1) | §5.1 |
| 6 | Бюджет: 2 сервера (€16–40/мес) + CF LB ($5–15/мес) + S3 (~$1–5/мес) — подтверждён? | §3.1 |
| 7 | Кто отвечает на алерт ночью (пока промоушен ручной — §7.1 требует человека) | §7 |

---

## 13. Порядок работ и оценка

| Этап | Что | Зависимости | Оценка |
|---|---|---|---|
| 1 | Аудит существующих бэкапов панели и бота (что в архиве, heartbeat, вторая копия в S3) + бэкап BFF + тестовое восстановление (У1) | доступ к серверу панели — **есть с 2026-08-06** (§0.2) | 1 день |
| 2 | Покупка B и C, WireGuard | ответ на вопрос 6 | 0.5 дня |
| 3 | Репликация Postgres A→B, A→C | этап 2, вопрос 5 | 1 день |
| 4 | Идентичность панелей: секреты, серты DNS-01, sub-origin | вопросы 1, 5 | 1 день |
| 5 | Firewall нод + CF LB + алерты | вопросы 1, 3 | 0.5–1 день |
| 6 | Ранбук + promote.sh + мониторинг §9 | этапы 3–5 | 1 день |
| 7 | Учения У2–У6 | всё выше | 1 день + окно |
| 8 | Третий узел бота | вопрос 4 | 1–2 дня |
| — | **Итого** | | **~7–9 рабочих дней** |
| 9 | (Этап 2, по решению после учений) Patroni-автопромоушен | этап 7 | 3–5 дней |
| 10 | (Отдельная задача) Резерв BFF | этап 7 | 2–3 дня |

Критический путь — ответы на вопросы 1 и 6; всё остальное в наших руках. Этап 1 не
ждёт ничего и начинается немедленно: с 2026-08-06 есть root на сервер панели (§0.2).
Перед всем остальным — **продление обоих серверов** (оплачены до 16.08.2026, §0.2).

---

## 14. Чего этот план сознательно не обещает

Чтобы ожидания заказчика совпали с реальностью:

- **«Ноль простоя» не бывает.** Реалистичная цель этой схемы: обнаружение < 2 мин,
  сервис восстановлен < 15 мин (ручной промоушен) или < 2 мин (после этапа
  Patroni). Уже подключённые VPN-клиенты простоя панели вообще не замечают.
- **Потеря последних секунд записей возможна.** Асинхронная репликация: транзакции,
  закоммиченные на A в последние секунды перед смертью, могли не доехать до B.
  Синхронная репликация это лечит ценой того, что смерть standby останавливает
  запись на живом primary — для этой системы размен невыгоден.
- **Переключение не мгновенное для всех.** DNS-кэши уважают не все резолверы; TTL
  30 с означает «большинство клиентов за минуту», не «все за 30 секунд».
- **Схема требует дисциплины эксплуатации:** обновление панели — на всех трёх в
  один день; новая нода — сразу три firewall-правила; квартальное учение У2.
  Нарушение любого из трёх тихо превращает резерв в декорацию — это главный
  долгосрочный риск всей конструкции.
