# FatVPN — план технических доработок (сводка)

> **Что это.** Полный технический аудит проекта от 2026-07-27 по коду ветки `master`,
> оформленный как техзадание для исполнителя (Claude Opus). Охвачены три части системы:
> BFF (.NET 10), Flutter-приложение с Android-нативом, iOS (Network Extension + sing-box).
>
> **Как пользоваться.** Каждая находка имеет номер, файл со строкой, объяснение «почему
> это плохо» и предлагаемое исправление. Номера стабильны — на них можно ссылаться в
> задачах и коммитах. Порядок работ предложен в конце каждого документа.

## Статус на 2026-07-28

Ветка `fix/bff-audit-remediation`.

**BFF — закрыт.** `820b1fe` закрывает все 6 critical и все 9 high, плюс почти весь
medium-хвост: атомарные claim'ы вместо read-modify-write в `/pair/status`,
`/auth/refresh` и привязке ключа к телефону; grace-окно 30 с на refresh; нормализация
`DateTimeOffset` от бота; per-IP rate limiting с `ForwardedHeaders`; валидация
`attestationToken`; fail-fast на секретах; кеш `/servers`, индексы, фоновая очистка
таблиц; тесты переехали на SQLite + Testcontainers (102 теста). Открытыми осознанно
оставлены **S2** (HTTPS — ждёт домена), **S9** (проверка владения в `/internal/*` —
требует правки бота) и остаток **S10** (лимит размера ответа панели).

**Android / Flutter — закрыт статически, но всё ещё в рабочем дереве.** Закоммичено:
release keystore (`cfe1f2b`), `allowBackup="false"` + переживание нечитаемого secure
storage (`901ea73`). **Не закоммичено** (проверено по диффу): секрет для clash API на
обеих платформах, `await` при сохранении ротированного refresh, disconnect при sign-out,
идемпотентный `_ensureInitialized`, нативный внетуннельный пинг на Android через
`VpnService.protect` (его пришлось написать с нуля), исключение x86/x86_64 из релиза,
`network_security_config` вместо `usesCleartextTraffic`, подтверждение ключа из
deep-link, единый `ApiClient` с кешем и рефрешем по сроку токена, устойчивый парсер
`/config` и приоритет схем, 402 больше не проглатывается, бэк-офф и стоп-условия для
`/pair/status`, `ApiException` без прозы, параллельные пинги с бюджетом 6, точечная
перерисовка таймера сессии, событийное ожидание disconnect, `stderr.log` во внутреннем
хранилище, flush логов, отказ от `SCHEDULE_EXACT_ALARM`, санитайзер диагностики,
`encryptedSharedPreferences`, тик уведомления 3 с со skip-if-unchanged, адаптивный
интервал watchdog'а, пакетные чтения storage, WEBP-иконки, cleanup `MethodChannel`.

**Измерено:** `flutter analyze` чисто в обоих пакетах; **275 тестов** зелёные (107 passed
+ 1 намеренный skip в `app/`, 168 в плагине), три прогона подряд без флаков.
`flutter build appbundle --release` подписан настоящим ключом, **скачивание 35.0 МБ на
arm64 / 34.6 МБ на arm32** при лимите Play 200 МБ на base-модуль.

**Осталось открытым:** certificate pinning (§1.2, ждёт HTTPS), Play Integrity (§1.8, не
брали), ленивая догрузка иконок сплит-туннеля (§3.11, отложена как переписывание экрана),
проброс вердикта нативного watchdog'а в Dart (§3.13, новая нативная поверхность на обеих
платформах — без устройства не проверить) и пересборка libbox без лишних build-тегов
(нужен Go-тулчейн). Пофайловый разбор, две находки сверх аудита (**N1**, **N2**) и три
места, где **ошибался сам аудит**, — в `improvement-plan-app-android.md`.

**Где ошибся сам аудит** (исправлено в тексте находок, а не просто помечено):
§1.4 успела устареть между аудитом и работами — `901ea73` закрыл её раньше;
§2.7 неверно винила URL-safe алфавит — декодер Dart его принимает, ломали только
padding и переносы строк; §2.5 недооценила объём — `pingServerOutsideTunnel` на Android
**не существовал**, и предложенное «убрать ветвление» уронило бы каждый пинг в
`MissingPluginException`. Плюс две находки, которых в аудите не было вовсе (**N1** —
параллельные пинги оказались медленнее последовательных из-за однопоточного нативного
executor'а; **N2** — кеш из §3.4 тихо сломал «живой» подсчёт нагрузки нод).

**iOS — закрыт в коде, но Swift здесь ни разу не компилировался.** Взяты все 30 находок:
три critical (blackhole при `serviceStop` — теперь `shutdownTunnel` с
`cancelTunnelWithError`; окно утечки в `clearDNSCache` — сброс настроек в `nil` убран,
`reasserting` снимается всегда; on-demand и kill-switch — новые переключатели в
настройках, прокинутые через четыре слоя до `NEOnDemandRuleConnect` и
`includeAllNetworks`), таймауты на `runBlocking`, серийная очередь под `tunnelOptions` и
флаг паузы ядра, удаление снапшота `start_options.plist` при остановке и логауте,
`matchDomains = [""]`, отказ от `fatalError` в расширении, `getdtablesize()`, удаление
plist-ветки `handleAppMessage`, редакторы секретов в обеих Swift-единицах, запрет
`logLevel: debug` в release, чтение `stderr.log` хвостом через `FileHandle`,
`logMaxLines` 3000 → 300, `memoryLimit` на iOS, watchdog 60 → 180 с с адаптацией по
`isExpensive`, `urlCache = nil`, жёсткое требование loopback для `external_controller`,
плюс `codemagic.yaml`: `flutter: 3.44.4`, `xcodeproj -v 1.27.0`, гейт
`flutter analyze && flutter test` в двух пакетах, проверка `codesign -d --entitlements`
и вынос отладочного шага из релизного пайплайна.

⚠️ **Статус этих правок — «в коде», и это не то же самое, что «исправлено».** В
окружении нет Xcode: ни одна из четырёх изменённых Swift-единиц не проверена даже на
сборку, а на устройстве не проверено вообще ничего. Первый прогон Codemagic — это и
есть первая проверка.

🔎 **Ремедиация вычитана повторно (2026-07-28), и в ней самой нашлось 30 пунктов** —
16 исправлено на месте, 14 открыто. Раздел «Вычитка ремедиации» в
`improvement-plan-ios.md`. Самое существенное:
- **Гейт качества в CI не блокировал сборку** — у шага `Analyze and test` не было
  `set -e`, поэтому падение `flutter analyze` печаталось и игнорировалось, а стопорить
  публикацию могла только последняя из пяти команд. Исправлено.
- **`FlutterEventSink` дёргался не с platform-thread** (крэш в debug, UB в release).
  Исправлено.
- **Проверка App Group entitlement смотрела не на тот бандл** — на архивный, а не на
  экспортированный `.ipa`, который как раз пере-подписывается; ненайденный бандл давал
  зелёный шаг. Исправлено.
- **Логаут по 401 и истечение подписки (402) не стирают persisted state** и на 402 не
  останавливают туннель — то есть 1.6/2.1 закрыты не на всех путях. **Открыто**, самый
  тяжёлый из оставшихся: на iOS с on-demand это подъём туннеля на отозванных кредах.
- **Восьми пунктов приёмки не хватало** — 14 изменений поведения не были покрыты ничем.
  Добавлены T26–T36.

**Три Dart-правки намеренно включены только на iOS** (`defaultTargetPlatform`): TUN-стек
`system` вместо gvisor, MTU 1280 и IPv6-адрес TUN-интерфейса. Файл общий с Android, но
released-сборка Android device-тестирована на gvisor/1100/IPv4-only — перенос туда
отдельная задача со своим прогоном.

**Осталось открытым на iOS:** Keychain для `configContent` (2.1 — нужен entitlement
`keychain-access-groups` и перевыпуск профилей, ошибка ломает старт туннеля; файловая
защита при этом сделана), рандомизация порта Clash API (2.2), MTU в настройках (3.4),
проброс вердикта нативного watchdog'а в Dart (3.5 — то же отложено и на Android), плюс
14 пунктов вычитки (V17–V30), из которых V17 — блокирующий по существу.

✅ Валидация `external_controller` на Dart-стороне (2.3) **закрыта** — эта строка
держалась в документах по ошибке: `_enforceLoopbackControlApi` существует, вызывается
после merge `rawConfigPatch` и покрыт тестами.

**Где ошибся аудит по iOS** (исправлено в тексте находок): **1.7** — предложенный фикс
делал поведение определённым, но утечку не закрывал, настоящая причина была в отсутствии
inet6-адреса у TUN; **1.14** — вторая половина рецепта невыполнима, iOS не сообщает имя
`utun` до открытия fd; **4.1** — рекомендация `xcode: 16.2` вредна, Apple ежегодно
поднимает минимальный SDK и пин падает **на аплоаде**; **4.4** — уже была закрыта, оба
iOS-воркфлоу стояли на 120; **2.4** — Dart-половина закрыта раньше, Android-доработками.
Плюс находка сверх аудита (**N1**): on-demand приходится снимать **перед** остановкой,
иначе система немедленно поднимает туннель обратно и кнопка «Отключить» не работает вовсе.

**Что блокирует релиз:**

1. **HTTPS-домен** — единственный блокер, общий для всех трёх сторон. На BFF всё готово
   за флагом `Security:RequireHttps`; в приложении — за одной строкой `bffBaseUrl` и
   двумя записями в `network_security_config.xml`; на iOS дополнительно нужен
   `NSAppTransportSecurity` (без HTTPS iOS-сборка вообще не достучится до BFF).
2. **iOS: сборка ни разу не запускалась.** Blackhole-баги (1.1, 1.2) и отсутствие
   on-demand (1.3) **исправлены в коде**, но Swift здесь не компилировался — нет Xcode.
   До зелёного прогона `ios-release` в Codemagic про эти находки нельзя сказать ничего:
   правки затрагивают четыре Swift-единицы разом, включая жизненный цикл расширения.
   Прогон заодно проверит три новых вещи сразу: доступность `flutter: 3.44.4` на
   раннере, гейт `flutter analyze && flutter test` и шаг
   `codesign -d --entitlements` — последний может оказаться первым, кто покажет, что у
   App ID Runner'а нет capability App Groups (iOS 1.9, 2.5).
3. **Приёмка на устройстве не проведена — ни на Android, ни на iOS.** Раздел «2a» в
   `docs/release-test-checklist.md` отмечен только в статической части (T1–T3).
   *Android:* всё переписанное живёт за platform channel'ами, куда хост-тесты не
   достают — подключение как таковое, смена сервера и авто-переключение, sign-out при
   живом туннеле, холодный старт после kill во время рефреша, диалог `fatvpn://`,
   санитайзер support-bundle, **обновление поверх старой установки** (сменился бэкенд
   secure storage) и то, что release-APK больше не ставится на x86_64-эмулятор.
   *iOS:* восемь пунктов блока T-iOS (T12–T16, T23–T25). Пять из них добавлены
   2026-07-28 именно потому, что довалидация Фазы 7 (2026-07-24) их не покрывала — и
   поэтому пропустила находки 1.1, 1.2, 1.3, 1.6 и 2.1.
4. ⛔ **`TRIAL_DEVICE_KEY_SALT`** должен быть задан в окружении контейнера **до** деплоя
   `820b1fe`, иначе BFF уйдёт в crash-loop.

> Размер AAB из этого списка **ушёл**: измерен, в лимиты Play укладывается с запасом.

| Документ | Область | Находок | Статус |
|---|---|---|---|
| [improvement-plan-bff.md](improvement-plan-bff.md) | Backend .NET 10 (`backend/`) | 6 critical, 9 high, 10 medium + low | ✅ закрыт `820b1fe`, кроме HTTPS (S2), владения в `/internal/*` (S9) и лимита ответа панели (S10) |
| [improvement-plan-app-android.md](improvement-plan-app-android.md) | Flutter + Android (`app/lib`, `app/android`, `app/packages/singbox_mm`) | 5 critical, 12 high, 20 medium, 8 low | ✅ закрыт статически (275 тестов, analyze чисто), но **в рабочем дереве** и **без единой проверки на устройстве**; статус по каждой находке — в шапке документа |
| [improvement-plan-ios.md](improvement-plan-ios.md) | iOS (`app/ios`, PacketTunnel NE) | 3 critical, 7 high, 12 medium, 8 low | 🟡 закрыт **в коде**, но **Swift ни разу не компилировался** (нет Xcode) и на устройстве не проверено ничего; 2.1 и 2.2 частично, четыре находки аудита оказались неверны; статус по каждой — в шапке документа |

> ⚠️ Ранее существовавший `docs/security-audit.md` (2026-07-25) **добавлен в `.gitignore`**
> и не попадает в репозиторий. Все его пункты учтены и перенесены в
> `improvement-plan-bff.md` (S1–S10), так что для работы он не нужен.

---

## Топ-10: что чинить в первую очередь

Отсортировано по «ущерб × вероятность», а не по формальной критичности.

| # | Что | Где | Почему первым | Статус |
|---|---|---|---|---|
| 1 | **HTTP вместо HTTPS** | `api_config.dart:4`, `Program.cs:61-65` | Refresh-токен на 90 дней и вся подписка (UUID, пароли) идут открытым текстом. Перехват = полный угон аккаунта. Для VPN-продукта разрушает саму модель угроз | ⬜ **блокер релиза**: нет домена. На BFF всё готово за флагом `Security:RequireHttps` |
| 2 | **Release подписан debug-ключом** | `build.gradle.kts:31-34` | Публично известный ключ: подмена приложения через sideload + доступ к signature-permission. В Google Play не загрузить | ✅ `cfe1f2b` |
| 3 | **APK ~236 МБ** | `app/android/app/build.gradle.kts` | Лимит Play — 100 МБ APK / 200 МБ AAB. Сборка физически не публикуется. x86/x86_64 = 54% веса и нужны только эмулятору | ✅ **измерено**: скачивание 35.0 МБ arm64 / 34.6 МБ arm32. ⚠️ Способ из плана (`ndk { abiFilters }`) не работает — сделано через Variant API |
| 4 | **Clash API без секрета на loopback** | `singbox_config_builder.dart:129-133` | Любое приложение на устройстве читает историю доменов пользователя в реальном времени и может выключить проксирование. Общая дыра Android + iOS | ✅ рабочее дерево — случайный секрет на каждый старт туннеля, все три потребителя его предъявляют (закрывает и iOS 2.2) |
| 5 | **Потеря ротированного refresh-токена** | `auth_controller.dart:204` + `AuthController.cs:62-113` | `unawaited(save)` + reuse-detection на сервере = пользователь теряет платную подписку после неудачно совпавшего kill процесса. Чинится с обеих сторон | ✅ обе стороны: BFF `820b1fe` (атомарная ротация + grace-окно 30 с), приложение — `await` перед сменой состояния (рабочее дерево). Проверяется пунктом T6 чек-листа |
| 6 | **`serviceStop` не отменяет туннель (iOS)** | `ExtensionPlatformInterface.swift:361-363` | Интернет пропадает целиком, приложение показывает «connected». Выход только ручным тоглом | 🟡 в коде: пути разведены, `shutdownTunnel()` вызывает `cancelTunnelWithError(nil)`. **Swift не компилировался**, на устройстве не проверено — T15 |
| 7 | **`clearDNSCache` снимает маршруты (iOS)** | `ExtensionPlatformInterface.swift:180-188` | Окно утечки трафика мимо VPN ровно при смене Wi-Fi↔LTE — самый частый сценарий | 🟡 в коде: сброс в `nil` убран, настройки применяются одним вызовом, `reasserting` снимается и на ошибке. **Не компилировалось**, проверяется T14 — критерий по логу расширения |
| 8 | **Гонки на BFF: `/pair/status`, `/auth/refresh`** | `PairController.cs:78-93`, `AuthController.cs:62-113` | Одноразовый код выдаёт две сессии; reuse-detection обходится гонкой. Проявляется как «случайные разлогины» | ✅ `820b1fe` |
| 9 | **Нет rate limiting** | `Program.cs` | `/trial` создаёт реального пользователя в панели на каждый вызов, без лимита и с обходимым anti-abuse. Порт публичный | ✅ `820b1fe` |
| 10 | **Пустой `attestationToken` = чужой триал** | `TrialController.cs:25` | `{"attestationToken":""}` даёт валидную сессию к триалу первого клиента. Одна строка валидации | ✅ `820b1fe` |

---

## Сквозные темы (чинить согласованно на двух сторонах)

Три пары находок нельзя закрывать по отдельности — иначе фикс на одной стороне ломает
или не даёт эффекта на другой:

**HTTPS.** BFF S2 (`UseHttpsRedirection` включён только в Development — условие
инвертировано) + Android §1.1 (`usesCleartextTraffic="true"`) + iOS (в `Info.plist` нет
`NSAppTransportSecurity`, то есть **iOS-сборка вообще не достучится до BFF по HTTP** —
скрытый блокер релиза). Плюс BFF S8: без `ForwardedHeaders` за Caddy редирект даст цикл,
а per-IP rate limiter схлопнет всех клиентов в один бакет.

> 🟡 **Состояние:** BFF-половина готова (`820b1fe`): `ForwardedHeaders` включены,
> `UseHttpsRedirection`/`UseHsts` живут за флагом `Security:RequireHttps` (по умолчанию
> выключен, иначе уже установленные сборки упрутся в 307). Осталось единственное и
> общее для всех трёх сторон — **домен с сертификатом**. Флаг, `AllowedHosts`,
> `bffBaseUrl` в приложении и `NSAppTransportSecurity` на iOS переключаются одним
> согласованным шагом.
> ⚠️ **Уточнение 2026-07-28: формулировка «скрытый блокер» скорее всего неверна.**
> `NSAppTransportSecurity` в `app/ios/Runner/Info.plist` действительно **отсутствует** —
> проверено, iOS-доработки его не касались. Но вывод «iOS-сборка вообще не достучится до
> BFF по HTTP» противоречит собственному журналу проекта: на builds 16–18 (2026-07-24)
> на реальном iPhone работали и авторизация, и список серверов — против того же
> `http://87.121.221.229:5030`. Причина в том, что ATS ограничивает `NSURLSession`, а
> `package:http` (`api_client.dart:3`) на iOS ходит через `dart:io HttpClient`, то есть
> по обычным сокетам, мимо ATS.
> Практический вывод не меняется — HTTPS нужен, — но **приоритет другой**: это не
> «iOS не заработает без ATS», а «на iOS открытый HTTP не ловится даже платформенной
> защитой». Перед тем как закладывать `NSAppTransportSecurity` в план миграции, стоит
> проверить, нужен ли он вообще при текущем HTTP-стеке.

**Refresh-токены.** Приложение §2.1 (сохранять на диск до смены состояния в памяти) +
BFF B2 (атомарный claim + **grace window ~10-30 с** для повторного предъявления только
что отротированного токена). Без grace window гонки мобильного клиента будут и дальше
приводить к отзыву семьи. Сюда же приложение §3.4: `AuthSession.expiresAt` — это срок
**подписки**, а не токена, поэтому приложение рефрешит по десятку раз в день на ровном
месте; BFF должен отдавать `accessTokenExpiresAt` отдельным полем.

> ✅ **BFF-половина сделана (`820b1fe`):** ротация атомарна, grace-окно —
> **30 с** (`Jwt:RefreshGraceWindow`, настраивается). В окно семья **не** отзывается,
> но выдаётся **новый** токен той же семьи, а не повтор токена победителя гонки —
> хранятся только хеши. Приложению достаточно сохранять последний полученный refresh.
> `/auth/token` и `/auth/refresh` теперь отдают `accessTokenExpiresAt` и
> `subscriptionExpiresAt` рядом со старым `expiresAt`; **`/pair/status` и `/trial` их
> не отдают** — приложению нужен фолбэк (см. `docs/api-contract.md`).

**Clash API.** Один фикс в общем Dart-файле `singbox_config_builder.dart` закрывает
Android §1.6 и iOS 2.2, но требует парных правок в трёх потребителях:
`VpnTunnelHealthProbe.kt:154`, `TunnelHealthWatchdog.swift:311-323` и
`vpn_controller.dart:458-482`. Плюс iOS 2.3 — валидировать, что `external_controller`
указывает на loopback (иначе сетевое расширение шлёт запросы на произвольный хост).

> ✅ **Состояние на 2026-07-28 (после вычитки):** секрет сделан и доходит до всех трёх
> потребителей. **iOS 2.3 закрыт полностью, обе половины.** Расширение жёстко требует
> loopback и пишет `refusing non-loopback control API`; Dart-сторона —
> `_enforceLoopbackControlApi` в `singbox_config_builder.dart`, вызывается **после**
> merge `rawConfigPatch` (патч и есть то единственное, что может увести контроллер),
> переписывает адрес в `127.0.0.1:$port`, а непарсящийся порт роняет `clash_api` целиком.
> Ранее здесь было написано «закрыт наполовину» — это утверждение устарело.
> Порт по-прежнему фиксированный (`16756`) — открыто.
>
> 🆕 Вычитка нашла, что `"::1"` проходил валидацию в расширении, но без скобок давал
> невалидный URL → каждая проба «unknown» → **watchdog молча переставал чинить туннель**.
> Исправлено (`[::1]`).
>
> ⚠️ **Связка с kill-switch (T23):** по коду опасение выглядит завышенным —
> `includeAllNetworks` влияет на выбор интерфейса для исходящего трафика, а watchdog
> ходит на loopback внутри того же процесса. Проверять стоит, но не как блокер.

Отдельно: файл `singbox_inbound_builder.dart` тоже общий — правка TUN-стека (iOS 3.2,
`system` молча превращается в `gvisor`) затронет Android, это нужно проверить отдельно.

> 🟡 **Состояние на 2026-07-28:** предупреждение учтено — правки в общем файле сделаны
> **платформенными**, а не глобальными. TUN-стек `system` (3.2), MTU 1280 (3.4) и
> inet6-адрес TUN (1.7) включаются по `defaultTargetPlatform == TargetPlatform.iOS`
> (`singbox_feature_settings.dart:11-63`); Android остаётся на gvisor/1100/IPv4-only,
> потому что released-сборка device-тестирована именно в этом виде. Перенос на Android —
> отдельная задача со своим прогоном на устройстве, и дыра 1.7 (IPv6 мимо туннеля) там
> **открыта**.

---

## Порядок работ

**Этап 1 — блокеры релиза.** Всё, без чего приложение нельзя опубликовать:
HTTPS-домен для BFF (BFF S2, S4, S8), release keystore, AAB с `abiFilters`,
`allowBackup="false"`, `await` при сохранении refresh-токена.
— 🟡 S4/S8 закрыты `820b1fe`, keystore — `cfe1f2b`; **HTTPS-домен остаётся блокером**.

**Этап 2 — безопасность.** Clash API secret, rate limiting на BFF, валидация
`attestationToken`, certificate pinning, `stderr.log` из внешнего хранилища, хранение
конфига iOS в Keychain, санитайзер диагностики перед показом и шарингом.
— 🟡 rate limiting и валидация `attestationToken` закрыты `820b1fe`; pinning ждёт HTTPS.
**Хранение конфига iOS в Keychain — единственный пункт этапа, оставленный открытым
сознательно** (iOS 2.1): нужен entitlement `keychain-access-groups` на обоих таргетах и
перевыпуск профилей, а ошибка там ломает не диагностику, а старт туннеля. Вместо него
сделана файловая защита: `completeFileProtectionUntilFirstUserAuthentication`,
`isExcludedFromBackup` и удаление всех артефактов при disconnect/логауте. Санитайзер
диагностики закрыт с обеих сторон — Dart раньше, обе Swift-единицы теперь.

**Этап 3 — корректность.** Гонки на BFF (`ExecuteUpdateAsync` вместо read-modify-write),
iOS blackhole-баги (1.1, 1.2, 1.4, 1.5), Android `_ensureInitialized` и disconnect при
sign-out, нормализация `DateTimeOffset` от бота.
— 🟡 BFF-половина (гонки + `DateTimeOffset`) закрыта `820b1fe`; Android-половина — в
рабочем дереве. **iOS blackhole-баги исправлены в коде, но Swift не компилировался** —
до зелёного прогона Codemagic и проверки на устройстве (T14, T15) этап не закрыт.

**Этап 4 — производительность.** Кеш `/servers` и индексы БД на BFF, параллельные пинги
и единый `ApiClient` в приложении, TUN-стек и MTU на iOS, тик уведомления и перерисовки UI.
— 🟡 BFF-половина (кеш, индексы, фоновая очистка, таймаут HttpClient) закрыта `820b1fe`.
**TUN-стек и MTU на iOS сделаны** (`system` вместо gvisor, MTU 1280) — и это самая
рискованная правка всего аудита: меняется путь обработки каждого пакета. Мерить на
устройстве надо не «работает ли», а скорость, стабильность длинной сессии и **пропали ли
jetsam-килы** по сравнению с gvisor (T23).

**Этап 5 — гигиена и долг.** Локализация сообщений, Testcontainers для тестов на
параллелизм, закрепление тулчейна в Codemagic, quality gate перед TestFlight,
адаптивные интервалы health-check.
— 🟡 Testcontainers закрыты `820b1fe`; локализация — в рабочем дереве (Android §2.10).
**Codemagic закрыт:** `flutter: 3.44.4` и `xcodeproj -v 1.27.0` закреплены, добавлен
гейт `flutter analyze && flutter test` в двух пакетах, отладочный `-showBuildSettings`
вынесен в `ios-unsigned`, поднят `max_build_duration` у `ios-libbox-xcframework`.
⚠️ **Xcode сознательно оставлен на `latest`** — вопреки рекомендации аудита: Apple
ежегодно поднимает минимальный SDK, принимаемый App Store Connect, и пин ломается **на
аплоаде**, после полной сборки. **Адаптивные интервалы health-check сделаны на обеих
платформах** (Android §3.12, iOS 3.5).

---

## Что уже сделано хорошо (не переделывать)

Аудит отдельно зафиксировал сильные места, чтобы исполнитель не «улучшил» работающее:

- **BFF:** refresh-токены хранятся хэшами, ротация + reuse-detection, constant-time
  сравнение bot-секрета, CSPRNG везде, одноразовые pairing-коды, чистая семантика
  401 vs 402, `CancellationToken` пробрасывается сквозь все слои.
- **Android:** защита broadcast'ов signature-permission + `RECEIVER_NOT_EXPORTED`,
  `VpnServiceLiveness` (решает «на диске connected, а процесс убит»), атомарная запись
  конфига с `fsync` + 0600, `AutoSwitchPolicy` со strikes и cooldown (покрыта тестами),
  различение `UNKNOWN` и `DEAD` в health-probe, watchdog на `Handler` а не `AlarmManager`
  (устройство не будится), `WakeLock` не используется вовсе.
- **iOS:** архитектура канонически верная (NEPacketTunnelProvider + Libbox CommandServer
  + `getsockopt(UTUN_OPT_IFNAME)`), `tcpLatencyMs` в расширении написан правильно
  (серийная очередь, `settled`-guard, `cancel()` во всех ветках) — его и надо перенести
  в плагин. Секретов в `codemagic.yaml` и в git нет: подпись через интеграцию Codemagic.
  > ✅ Перенос `tcpLatencyMs` в плагин сделан (iOS 1.11). Остальное не трогали.

---

## Проверка результата

- **BFF:** `dotnet build` + `dotnet test` зелёные; добавлены интеграционные тесты на
  параллельные `/pair/status`, `/auth/refresh`, `/trial` (текущие тесты на EF InMemory
  **не ловят ни одну гонку** — см. BFF B11).
- **Приложение:** `flutter analyze` без ошибок, `flutter test` зелёный, release-сборка
  укладывается в лимиты Play.
- **На устройстве:** подключение, смена сервера, sign-out при активном туннеле (туннель
  должен опуститься), холодный старт после kill процесса во время рефреша.
- **iOS на устройстве:** DNS-leak, **IPv6-leak** (новый пункт — валидация Фазы 7 его не
  проверяла, поэтому находки 1.2 и 1.7 прошли незамеченными), смена Wi-Fi↔LTE под
  нагрузкой, отключение через UI действительно возвращает интернет.
  ✅ **Расширено 2026-07-28:** блок **T-iOS** в `docs/release-test-checklist.md` — восемь
  пунктов с явными «как проверить» и «что считается провалом»: **T12** IPv6-leak,
  **T13** DNS-leak (три прогона, а не один), **T14** смена сети под нагрузкой,
  **T15** отключение возвращает интернет, **T16** подъём после перезагрузки,
  **T23** кил расширения (jetsam), **T24** что осталось на диске после logout,
  **T25** support bundle без UUID/паролей/доменов. Пять из восьми — новые, добавлены
  под находки 1.3, 1.6, 2.1 и 2.4.
- **iOS до устройства:** зелёная сборка `ios-release` в Codemagic. Сегодня её не было ни
  разу с этими правками — Swift здесь не компилировался.
- В `docs/ios-vpn-tunnel-spec.md` дописано, чего именно довалидация Фазы 7 не покрывала и
  почему пять находок прошли через неё незамеченными.
